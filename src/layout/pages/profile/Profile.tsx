import React from 'react';

interface Props {}

const Profile = (props: Props) => {
    return (
        <div className="Profile">
            Profile
        </div>
    );
};

export default Profile;